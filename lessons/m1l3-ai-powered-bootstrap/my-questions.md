# m1l3 — Pytania na Q&A

- **Stan pośredni → YOLO: realny instynkt.** Prowadzący od września 2025 pracują głównie z `--dangerously-skip-permissions`. Po jakim sygnale w praktyce warto się przesiadać? Konkretnie: ile sesji bez incydentu, czy obecność prod-MCP jako disqualifier, czy to bardziej "po MVP, w drugim projekcie kursowym, nie wcześniej"?
- **CLAUDE.md vs polityka uprawnień jako pierwsza linia obrony.** Z Deep Dive: prowadzący po incydencie z bazą dopisali regułę w `CLAUDE.md` ("nie wykonuj operacji destruktywnych na bazie bez interaktywnego potwierdzenia"). Czemu w `CLAUDE.md`, a nie w `deny` settings.json (np. `Bash(*drop database*)`)? W którym miejscu agent realnie respektuje takie ograniczenie skuteczniej?
- **HARD-STOP w bootstrapperze — najczęstsze przyczyny.** Skill zostawia `.bootstrap-scaffold/` i `verification.md` w `failed`. W 3. edycji: jakie 2-3 przyczyny widzicie najczęściej (brak sieci, lockfile, kolizja nazwy, brakujące env), i czy są takie, które kursanci błędnie próbują "naprawić" promptem zamiast na poziomie systemu?
- **`/10x-health-check` werdykt `critical-issues` — co realnie blokuje M1L4.** Czy mieliście kursanta, który mimo `critical-issues` przeszedł do M1L4 Agent Onboarding? Które braki są twardym blockerem, a które warto skontekstować w `AGENTS.md` i jechać dalej?
- **Probabilistyczność polityki uprawnień.** Lekcja przyznaje wprost, że są obejścia (`/proc/self/root/usr/bin/npx`, prośba modelu o ręczne wyłączenie sandboxa). Jakie defense-in-depth poza `.claude/settings.json` realnie zalecacie na ten etap kursu (oddzielny user systemowy, nspawn/firejail, sandboxed shell), czy świadomie zostawiacie to w kategorii "wiedz, że tak bywa, ale realnie godzisz się z ryzykiem"?
